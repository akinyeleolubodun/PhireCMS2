<!DOCTYPE html>
<!-- Header //-->
<html>

<head>
    <title><?php echo $title; ?></title>

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />

<?php echo ((isset($assets)) ? $assets : null); ?>
</head>
<body>
<?php if (isset($user) && isset($user->username)): ?>
    <p>
        <strong>Logged in as:</strong> <a href="<?php echo BASE_PATH . $user->typeUri; ?>/profile"><?php echo $user->username; ?></a> | <a href="<?php echo ((BASE_PATH . APP_URI) != '') ? BASE_PATH . APP_URI : '/'; ?>">Dashboard</a> | <a href="<?php echo BASE_PATH . $user->typeUri; ?>/logout">Logout</a><br />
        <span style="font-size: 0.9em;"><strong>Last Login:</strong><?php echo $user->last; ?></span>
    </p>
<?php endif; ?>
<?php if (isset($errors) && ($_SERVER['REQUEST_URI'] != BASE_PATH . APP_URI . '/login')): ?>
    <div id="errors">
        <p><strong>Some of the content folders are not writable. This may cause the system to not function properly.</strong> <a href="#" onclick="wipeErrors(this, <?php echo (50 + (substr_count($errors, '<br />') * 20)); ?>); return false;">Show</a> which folders are not writable. [ <a href="#" onclick="$fx('#errors').fade(0, 15, 30); return false;">Dismiss</a> ]</p>
        <p>
<?php echo $errors; ?>
        </p>
    </div>
<?php endif; ?>
